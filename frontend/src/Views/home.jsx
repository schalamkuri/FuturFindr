import React from "react";

function Home() {
  const styles = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  };

  return (
    <div style={styles}>
      <h2>Home</h2>
    </div>
  );
}

export default Home;
